export interface ErrorResponseType {
  error: string
}

export interface SuccessResponderType {
  _id: string,
  title: string,
  description: string,
  price: number,
  urlImage: string,
  whatsappGroupLink: string,
  telegramGroupLink: string,
}
